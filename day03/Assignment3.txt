1) Does ANSI recognize the data type DATE?
-->Yes,ANSI recognizess DATE as a data type. Format of type date is 'YYYY-MM-DD'

2) Which subdivision of SQL is used to insert values in tables?
-->Data Manipulation Language(DML) is subdivison of SQL that is capable of inserting, updating, deleting values into the table.